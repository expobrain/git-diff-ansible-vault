language: python

cache: pip

python:
  - "2.7"
  - "3.7"
  - "3.8"

env:
- ANSIBLE_VER=<2.8
- ANSIBLE_VER=>=2.9

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get -y install git
  - pip install "ansible$ANSIBLE_VER"
  - make setup

script:
  - make test
